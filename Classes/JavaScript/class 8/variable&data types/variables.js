// variable are containers that can store different values or items

// is a old style of writing or declaring variables
// eich way
// declaring & intializing variables
// re-declaring of same variable
// re-initializing variables
var a = 10;
console.log(a);
var a = "sambit";
console.log(a);

// ES2015

// let & const

// let allows to re-initialize a variable whereas it doesn't allow to
// re-declare it
let a = 10;
a = "ankit";

// const doesn't allow to re-initialize or re-declare a variable
const b = 10;
b = 20;
console.log(b);
